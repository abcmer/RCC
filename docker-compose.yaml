version: '3'
services:
  client:
    container_name: rcq-client
    image: rcq-client
    build: ./client
    ports:
      - "80:80"
  server:
    depends_on:
      - mongo
    container_name: rcq-server
    image: rcq-server
    build: ./server
    ports:
      - "5000:5000"
    environment: 
      - DB=mongodb://mongo:27017/rcq
  mongo:
    image: "mongo"
    container_name: "rcq-mongo"
    ports:
      - "27017:27017"
    volumes:
    - data-volume:/data/db
volumes:
  data-volume:
